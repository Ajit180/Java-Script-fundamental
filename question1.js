let number = 9; // You can change this number to test with other values

// Using the ternary operator to check if the number is even or odd
let result = (number % 2 === 0) ? "even" : "odd";

console.log(`The number ${number} is ${result}.`);
